version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
    environment:
      - PORT=8000
      - ENVIRONMENT=development
      - BACKEND_CORS_ORIGINS=["*"]
